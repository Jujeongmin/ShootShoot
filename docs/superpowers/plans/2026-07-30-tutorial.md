# 첫 플레이 튜토리얼 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 첫 판에서 기본 사격을 네 단계로 가르친다 — 조준, 발사, 장전, 라운드 클리어.

**Architecture:** 단계 전이는 three.js·DOM 을 모르는 순수 상태 기계가 갖고, "봤음" 기록은 별도 `localStorage` 스토어가 갖는다. `game.js` 는 네 지점에서 이벤트를 보내고 프레임 루프에서 현재 단계를 UI에 넘기기만 한다.

**Tech Stack:** Vite 5, vitest 1.6 (`environment: 'node'`), 바닐라 DOM.

## Global Constraints

- 작업 브랜치는 `master` 직접 커밋. worktree 사용 금지.
- 기존 164개 테스트는 전부 통과 상태를 유지한다. 실행: `npm test`
- `npm run build` 가 성공해야 한다.
- **진행을 강제하지 않는다.** 단계를 관찰만 하고 다른 조작을 막지 않는다.
- 단계가 기다리는 이벤트가 아니면 무시한다. 단계를 건너뛰지 않는다.
- UI는 `theme.css` 클래스와 CSS 변수를 쓴다. **hex 를 손으로 박지 않는다.**
- 문구는 UI 모듈이 갖는다. 상태 기계는 단계 이름만 내보낸다.
- 튜토리얼 문구는 화면 **하단 중앙**이다. 우측 상단은 `P` 힌트가, 화면 중앙은 조준선이 쓴다.
- 헤드샷·연속 배율·엄폐물·상점·업그레이드·바주카는 설명하지 않는다.
- 커밋 메시지는 영어로, 본문에 왜 그렇게 했는지를 적는다.

## 스펙에서 확인한 현재 구조

구현자가 알아야 할 사실들이다. 코드를 다시 뒤질 필요가 없도록 여기 모았다.

- 스토어 패턴: `createXStore(storage, key)` 가 객체를 돌려주고, 키는 `config.js` 아래쪽
  `highScoreStorageKey` ~ `bazookaStorageKey` 일곱 줄로 모여 있다.
- 스토어 테스트 패턴은 `test/currencyStore.test.js` 에 있다. 가짜 storage 를
  `createMemoryStorage()` 로 만드는데 **`getItem`/`setItem` 만 있고 `removeItem` 이 없다.**
  이 계획서의 스토어는 `removeItem` 을 쓰므로 가짜 storage 에 그것도 넣어야 한다.
- `game.js` 의 프레임 루프 첫 세 줄은 이렇다:
  ```js
      lastKillEffect.update(dt);
      reload.tick(dt);
      input.setEnabled(phase === 'playing' && !settingsOpen && !reload.isReloading());
  ```
  바로 아래에 `hud.setHintVisible(...)` 이 있다.
- `input.onAimDown(() => { aimApplied = false; resumeAudio(); });` 이 조준 시작 지점이다.
- `handleShot` 은 끝에서 `reload.start();` 와 `weaponViewmodel.triggerReload(...)` 를 부른다.
  거기까지 왔으면 실제로 발사된 것이다.
- 라운드 클리어 분기는 `if (targetManager.allCleared() && !projectiles.hasPending()) {` 이고
  첫 줄이 `currencyStore.earn(...)` 이다.
- `settingsPanel.show(sensitivity, onChange, onClose, onExit)` 이고 본문 끝에서
  `onExit` 이 함수일 때만 `메뉴로` 버튼을 붙인다. 호출부는 `openSettingsFromMenu`(인자 3개)와
  `openSettingsFromPlay`(인자 4개) 둘이다.
- `theme.css` 의 `:root` 에 `--k-font`, `--k-white`, `--k-ink` 가 있다.
- `hud.js` 가 `k-hint-key` / `k-hint-text` 로 우측 상단 `P` 힌트를 그린다. z-index 21.

## 파일 구조

**새로 만들 파일**

| 파일 | 책임 |
|---|---|
| `game/src/gameplay/tutorialState.js` | 단계와 전이만 아는 순수 상태 기계 |
| `test/tutorialState.test.js` | 위 모듈 테스트 |
| `game/src/gameplay/tutorialStore.js` | "봤음"을 `localStorage` 에 남긴다 |
| `test/tutorialStore.test.js` | 위 모듈 테스트 |
| `game/src/ui/tutorialPrompt.js` | 문구 한 줄을 화면에 띄운다 |

**수정할 파일**

| 파일 | 무엇을 |
|---|---|
| `game/src/config.js` | `tutorialStorageKey` 추가 |
| `game/src/ui/theme.css` | `.k-tutorial` 클래스 추가 |
| `game/src/ui/settingsPanel.js` | `onReplayTutorial` 를 받아 버튼 하나 더 |
| `game/src/gameplay/game.js` | 생성, 이벤트 네 곳, 프레임 루프, 다시 보기 핸들러 |

**건드리지 않는 파일**

`input.js`, `reloadState.js`, `shooting.js`, `scoring.js`, `obstacles.js`, `debris.js`, `hud.js`, `skyDecor.js`.

---

### Task 1: 단계 상태 기계

three.js 도 DOM 도 안 쓴다. vitest 의 node 환경에서 그대로 돈다.

**Files:**
- Create: `game/src/gameplay/tutorialState.js`
- Create: `test/tutorialState.test.js`

**Interfaces:**
- Consumes: 없음
- Produces (Task 3 이 쓴다): `createTutorialState() -> { current(), handle(event), isDone(), reset() }`
  - `current()` 는 `'aim' | 'fire' | 'reload' | 'clear' | 'done'`
  - `handle(event)` 는 `'aimStarted' | 'shotFired' | 'reloadFinished' | 'roundCleared'`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `test/tutorialState.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { createTutorialState } from '../game/src/gameplay/tutorialState.js';

function advanceToDone(tutorial) {
  tutorial.handle('aimStarted');
  tutorial.handle('shotFired');
  tutorial.handle('reloadFinished');
  tutorial.handle('roundCleared');
}

describe('createTutorialState', () => {
  it('starts on the aim step', () => {
    const tutorial = createTutorialState();
    expect(tutorial.current()).toBe('aim');
    expect(tutorial.isDone()).toBe(false);
  });

  it('advances from aim to fire when the aim starts', () => {
    const tutorial = createTutorialState();
    tutorial.handle('aimStarted');
    expect(tutorial.current()).toBe('fire');
  });

  it('walks all four steps in order and finishes', () => {
    const tutorial = createTutorialState();
    tutorial.handle('aimStarted');
    expect(tutorial.current()).toBe('fire');
    tutorial.handle('shotFired');
    expect(tutorial.current()).toBe('reload');
    tutorial.handle('reloadFinished');
    expect(tutorial.current()).toBe('clear');
    tutorial.handle('roundCleared');
    expect(tutorial.current()).toBe('done');
    expect(tutorial.isDone()).toBe(true);
  });

  it('ignores an event the current step is not waiting for', () => {
    const tutorial = createTutorialState();
    tutorial.handle('roundCleared');
    tutorial.handle('reloadFinished');
    tutorial.handle('shotFired');
    expect(tutorial.current()).toBe('aim');
  });

  it('stays on the same step when its own event repeats', () => {
    const tutorial = createTutorialState();
    tutorial.handle('aimStarted');
    tutorial.handle('aimStarted');
    expect(tutorial.current()).toBe('fire');
  });

  it('does nothing once it is done', () => {
    const tutorial = createTutorialState();
    advanceToDone(tutorial);
    tutorial.handle('aimStarted');
    tutorial.handle('roundCleared');
    expect(tutorial.current()).toBe('done');
    expect(tutorial.isDone()).toBe(true);
  });

  it('reset() returns to the first step', () => {
    const tutorial = createTutorialState();
    advanceToDone(tutorial);
    tutorial.reset();
    expect(tutorial.current()).toBe('aim');
    expect(tutorial.isDone()).toBe(false);
  });

  it('ignores an unknown event name', () => {
    const tutorial = createTutorialState();
    tutorial.handle('somethingElse');
    expect(tutorial.current()).toBe('aim');
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npx vitest run test/tutorialState.test.js`
Expected: FAIL — `Failed to resolve import "../game/src/gameplay/tutorialState.js"`

- [ ] **Step 3: 구현**

Create `game/src/gameplay/tutorialState.js`:

```js
// 튜토리얼 단계와 전이만 갖는다. 문구는 UI(tutorialPrompt.js)가 갖는다 — 문구를
// 고칠 때 이 모듈의 테스트가 깨지면 안 된다.
//
// 각 단계는 자기가 기다리는 이벤트 하나에만 반응한다. 그래서 조준을 여러 번
// 누르거나 엉뚱한 순서로 이벤트가 와도 단계가 건너뛰어지지 않는다.
const NEXT_STEP = {
  aim: { event: 'aimStarted', next: 'fire' },
  fire: { event: 'shotFired', next: 'reload' },
  reload: { event: 'reloadFinished', next: 'clear' },
  clear: { event: 'roundCleared', next: 'done' },
};

const FIRST_STEP = 'aim';
const DONE_STEP = 'done';

export function createTutorialState() {
  let step = FIRST_STEP;

  return {
    current() {
      return step;
    },
    handle(event) {
      const transition = NEXT_STEP[step];
      if (!transition) return;
      if (transition.event !== event) return;
      step = transition.next;
    },
    isDone() {
      return step === DONE_STEP;
    },
    reset() {
      step = FIRST_STEP;
    },
  };
}
```

`done` 에는 `NEXT_STEP` 항목이 없으므로 `handle` 이 첫 줄에서 빠져나간다 — 별도 분기가 필요 없다.

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run test/tutorialState.test.js`
Expected: PASS, 8 tests

- [ ] **Step 5: 전체 테스트와 빌드**

Run: `npm test`
Expected: 172 tests passed (164 + 8)

Run: `npm run build`
Expected: 성공

- [ ] **Step 6: 커밋**

```bash
git add game/src/gameplay/tutorialState.js test/tutorialState.test.js
git commit -m "feat: add the tutorial step machine

Each step reacts to exactly one event, so pressing aim twice or receiving
events out of order cannot skip a step — the wiring in game.js fires from
four unrelated places and cannot be trusted to arrive in order.

The step names come out but the wording does not: the prompt owns the text
so changing a sentence never breaks these tests.

The done state has no entry in the transition table, which is what makes
it absorb every later event without a branch of its own."
```

---

### Task 2: 봤음 기록 스토어

판을 넘어 남는 값이다. 판 안에서만 사는 단계와 분리해 두면 "다시 보기"가 무엇을
지워야 하는지가 분명해진다.

**Files:**
- Create: `game/src/gameplay/tutorialStore.js`
- Create: `test/tutorialStore.test.js`
- Modify: `game/src/config.js`

**Interfaces:**
- Consumes: 없음
- Produces (Task 3 이 쓴다): `createTutorialStore(storage, key) -> { isDone(), markDone(), clear() }`
  그리고 `CONFIG.tutorialStorageKey`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `test/tutorialStore.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { createTutorialStore } from '../game/src/gameplay/tutorialStore.js';

// 다른 스토어 테스트의 가짜 storage 에는 removeItem 이 없다. clear() 가 그것을
// 쓰므로 여기서는 셋을 다 갖춘다.
function createMemoryStorage() {
  const map = new Map();
  return {
    getItem: (k) => (map.has(k) ? map.get(k) : null),
    setItem: (k, v) => map.set(k, v),
    removeItem: (k) => map.delete(k),
  };
}

describe('createTutorialStore', () => {
  it('is not done when nothing is stored', () => {
    const store = createTutorialStore(createMemoryStorage(), 'test.tutorial');
    expect(store.isDone()).toBe(false);
  });

  it('is done after markDone()', () => {
    const store = createTutorialStore(createMemoryStorage(), 'test.tutorial');
    store.markDone();
    expect(store.isDone()).toBe(true);
  });

  it('is not done again after clear()', () => {
    const store = createTutorialStore(createMemoryStorage(), 'test.tutorial');
    store.markDone();
    store.clear();
    expect(store.isDone()).toBe(false);
  });

  it('treats any other stored value as not done', () => {
    const storage = createMemoryStorage();
    storage.setItem('test.tutorial', 'yes');
    const store = createTutorialStore(storage, 'test.tutorial');
    expect(store.isDone()).toBe(false);
  });

  it('markDone() twice leaves it done', () => {
    const store = createTutorialStore(createMemoryStorage(), 'test.tutorial');
    store.markDone();
    store.markDone();
    expect(store.isDone()).toBe(true);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npx vitest run test/tutorialStore.test.js`
Expected: FAIL — `Failed to resolve import "../game/src/gameplay/tutorialStore.js"`

- [ ] **Step 3: 구현**

Create `game/src/gameplay/tutorialStore.js`:

```js
// 튜토리얼을 봤는지만 기억한다. 판 안의 진행 단계는 tutorialState.js 가 갖는다 —
// 수명이 달라서 한 모듈에 두면 '다시 보기'가 무엇을 지워야 하는지 흐려진다.
const DONE_VALUE = '1';

export function createTutorialStore(storage, key) {
  return {
    isDone() {
      return storage.getItem(key) === DONE_VALUE;
    },
    markDone() {
      storage.setItem(key, DONE_VALUE);
    },
    clear() {
      storage.removeItem(key);
    },
  };
}
```

- [ ] **Step 4: `CONFIG` 에 키 추가**

`game/src/config.js` 의 `bazookaStorageKey: 'shootshoot.bazooka',` 바로 아래에 넣는다:

```js
  tutorialStorageKey: 'shootshoot.tutorial',
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `npx vitest run test/tutorialStore.test.js`
Expected: PASS, 5 tests

- [ ] **Step 6: 전체 테스트와 빌드**

Run: `npm test`
Expected: 177 tests passed (172 + 5)

Run: `npm run build`
Expected: 성공

- [ ] **Step 7: 커밋**

```bash
git add game/src/gameplay/tutorialStore.js test/tutorialStore.test.js game/src/config.js
git commit -m "feat: remember that the tutorial has been seen

Whether the player has seen the tutorial outlives a run, while the step
they are on does not. Keeping the two in separate modules is what makes
'replay tutorial' obvious: it clears this one and resets the other.

Only the exact string '1' counts as seen, so a stale or hand-edited value
in localStorage shows the tutorial rather than silently swallowing it."
```

---

### Task 3: 화면 문구와 배선

문구 UI를 만들고 `game.js`·`settingsPanel.js` 에 붙인다. 이 태스크가 끝나면 게임에서
눈으로 확인할 수 있다.

**Files:**
- Create: `game/src/ui/tutorialPrompt.js`
- Modify: `game/src/ui/theme.css`
- Modify: `game/src/ui/settingsPanel.js`
- Modify: `game/src/gameplay/game.js`

**Interfaces:**
- Consumes:
  - Task 1 의 `createTutorialState() -> { current(), handle(event), isDone(), reset() }`
  - Task 2 의 `createTutorialStore(storage, key) -> { isDone(), markDone(), clear() }` 와 `CONFIG.tutorialStorageKey`
- Produces:
  - `createTutorialPrompt(container) -> { show(step), dispose() }`
  - `settingsPanel.show(sensitivity, onChange, onClose, onExit, onReplayTutorial)` — 다섯 번째 인자가 함수일 때만 `튜토리얼 다시 보기` 버튼이 붙는다

- [ ] **Step 1: 문구 UI 만들기**

Create `game/src/ui/tutorialPrompt.js`:

```js
// 단계 이름을 문구로 바꾸는 곳. 상태 기계는 문구를 모른다.
const STEP_TEXT = {
  aim: '마우스를 누른 채로 조준하세요',
  fire: '손을 떼면 발사됩니다',
  reload: '장전 중에는 조준할 수 없습니다',
  clear: '원숭이를 모두 잡으면 다음 라운드로',
};

export function createTutorialPrompt(container) {
  const el = document.createElement('div');
  el.className = 'k-tutorial';
  el.style.display = 'none';
  container.appendChild(el);

  let shownStep = null;

  return {
    // 매 프레임 불린다. 단계가 그대로면 DOM 을 건드리지 않는다.
    show(step) {
      if (step === shownStep) return;
      shownStep = step;
      const text = STEP_TEXT[step];
      if (text === undefined) {
        el.style.display = 'none';
        return;
      }
      el.textContent = text;
      el.style.display = 'block';
    },
    dispose() {
      el.remove();
    },
  };
}
```

- [ ] **Step 2: `theme.css` 에 클래스 추가**

`game/src/ui/theme.css` 맨 끝에 붙인다:

```css

/* 튜토리얼 안내. 화면 하단 중앙에 둔다 — 중앙은 조준선이, 우측 상단은 P 힌트가
   쓰고 있어서 비켜야 한다. 3D 장면 위에 바로 얹히므로 외곽선 그림자로 띄운다. */
.k-tutorial {
  position: absolute;
  bottom: 40px;
  left: 50%;
  transform: translateX(-50%);
  max-width: 90%;
  text-align: center;
  font-family: var(--k-font);
  font-size: 22px;
  font-weight: bold;
  letter-spacing: 1px;
  color: var(--k-white);
  text-shadow:
    0 3px 0 var(--k-ink),
    0 0 6px rgba(0, 0, 0, 0.9);
  pointer-events: none;
  z-index: 21;
}
```

- [ ] **Step 3: 설정 패널에 다시 보기 버튼**

`game/src/ui/settingsPanel.js` 의 `show` 시그니처를 바꾼다. 기존:

```js
  function show(sensitivity, onChange, onClose, onExit) {
```
변경 후:
```js
  // onReplayTutorial 은 메뉴에서도 넘어온다. 조작 설명이라 '메뉴로'와 달리
  // 어디서 열었든 다시 볼 수 있어야 한다.
  function show(sensitivity, onChange, onClose, onExit, onReplayTutorial) {
```

같은 함수의 버튼 묶음을 바꾼다. 기존:

```js
    if (typeof onExit === 'function') {
      actions.appendChild(button('메뉴로', onExit, 'ghost'));
    }
    actions.appendChild(button('닫기', onClose, 'ghost'));
```
변경 후:
```js
    if (typeof onReplayTutorial === 'function') {
      actions.appendChild(button('튜토리얼 다시 보기', onReplayTutorial, 'ghost'));
    }
    if (typeof onExit === 'function') {
      actions.appendChild(button('메뉴로', onExit, 'ghost'));
    }
    actions.appendChild(button('닫기', onClose, 'ghost'));
```

- [ ] **Step 4: `game.js` 에 import 추가**

`game/src/gameplay/game.js` 의 import 묶음에 셋을 더한다. `import { createSkyDecor } from './skyDecor.js';` 아래에 둘:

```js
import { createTutorialState } from './tutorialState.js';
import { createTutorialStore } from './tutorialStore.js';
```

그리고 `import { createHud } from '../ui/hud.js';` 아래에:

```js
import { createTutorialPrompt } from '../ui/tutorialPrompt.js';
```

- [ ] **Step 5: 생성**

`createGame` 안, `const hud = createHud(container);` 바로 아래에 넣는다:

```js
  const tutorialPrompt = createTutorialPrompt(container);
```

그리고 `const bazookaStore = createBazookaStore(window.localStorage, CONFIG.bazookaStorageKey);`
바로 아래에 넣는다:

```js
  const tutorialStore = createTutorialStore(window.localStorage, CONFIG.tutorialStorageKey);
  const tutorial = createTutorialState();
```

- [ ] **Step 6: 장전 종료를 감지할 변수 추가**

상태 선언 묶음, `const reload = createReloadState(CONFIG.reload.seconds);` 바로 아래에 넣는다:

```js
  // 장전이 끝나는 프레임을 잡으려면 지난 프레임 값이 필요하다. 튜토리얼의
  // reloadFinished 가 이 내림 엣지에서 한 번만 나가야 한다.
  let wasReloading = false;
```

- [ ] **Step 7: 판 시작 시 튜토리얼 켜기**

`startGame()` 의 `reload.reset();` 바로 아래에 넣는다:

```js
    if (!tutorialStore.isDone()) tutorial.reset();
```

- [ ] **Step 8: 조준 시작 이벤트**

기존:

```js
  input.onAimDown(() => {
    aimApplied = false;
    resumeAudio();
  });
```
변경 후:
```js
  input.onAimDown(() => {
    aimApplied = false;
    resumeAudio();
    tutorial.handle('aimStarted');
  });
```

- [ ] **Step 9: 발사 이벤트**

`handleShot` 끝이다. 기존:

```js
    reload.start();
    weaponViewmodel.triggerReload(CONFIG.reload.seconds);
  }
```
변경 후:
```js
    reload.start();
    weaponViewmodel.triggerReload(CONFIG.reload.seconds);
    tutorial.handle('shotFired');
  }
```

- [ ] **Step 10: 프레임 루프 — 장전 종료 감지와 문구 갱신**

프레임 루프 첫머리다. 기존:

```js
      lastKillEffect.update(dt);
      reload.tick(dt);
      input.setEnabled(phase === 'playing' && !settingsOpen && !reload.isReloading());
      // 안내는 플레이 중에만 띄운다. 설정이 열려 있으면 패널 위로 삐져나오므로 숨긴다.
      hud.setHintVisible(phase === 'playing' && !settingsOpen);
```
변경 후:
```js
      lastKillEffect.update(dt);
      reload.tick(dt);
      const reloading = reload.isReloading();
      input.setEnabled(phase === 'playing' && !settingsOpen && !reloading);
      // 안내는 플레이 중에만 띄운다. 설정이 열려 있으면 패널 위로 삐져나오므로 숨긴다.
      hud.setHintVisible(phase === 'playing' && !settingsOpen);
      if (wasReloading && !reloading) {
        tutorial.handle('reloadFinished');
      }
      wasReloading = reloading;
      tutorialPrompt.show(phase === 'playing' && !settingsOpen ? tutorial.current() : 'done');
```

문구를 숨겨야 할 때 `'done'` 을 넘긴다 — `STEP_TEXT` 에 없는 이름이면 `show` 가 숨기므로
따로 숨기는 함수가 필요 없다.

- [ ] **Step 11: 라운드 클리어 이벤트**

라운드 클리어 분기다. 기존:

```js
        if (targetManager.allCleared() && !projectiles.hasPending()) {
          // 이 라운드에서 번 만큼만 지급한다. 도중에 나가면 정산을 안 하므로
          // 그 라운드 점수는 그대로 버려진다.
          currencyStore.earn(settlementGold(scoreState.score - settledScore, CONFIG.scorePerGold));
```
변경 후:
```js
        if (targetManager.allCleared() && !projectiles.hasPending()) {
          tutorial.handle('roundCleared');
          if (tutorial.isDone()) tutorialStore.markDone();
          // 이 라운드에서 번 만큼만 지급한다. 도중에 나가면 정산을 안 하므로
          // 그 라운드 점수는 그대로 버려진다.
          currencyStore.earn(settlementGold(scoreState.score - settledScore, CONFIG.scorePerGold));
```

- [ ] **Step 12: 다시 보기 핸들러와 설정 호출부 둘**

`closeSettings` 함수 바로 위에 핸들러를 넣는다:

```js
  // 기록을 지우고 단계를 처음으로 돌린다. 플레이 중에 눌렀으면 이 판에서 바로
  // 1단계가 보이고, 메뉴에서 눌렀으면 startGame 이 isDone()을 다시 보므로
  // 다음 판 시작 때 보인다.
  function replayTutorial() {
    tutorialStore.clear();
    tutorial.reset();
    closeSettings();
  }
```

그리고 호출부 둘을 바꾼다. 기존:

```js
    settingsPanel.show(sensitivity, handleSensitivityChange, closeSettings);
```
변경 후:
```js
    settingsPanel.show(sensitivity, handleSensitivityChange, closeSettings, undefined, replayTutorial);
```

기존:

```js
    settingsPanel.show(sensitivity, handleSensitivityChange, closeSettings, exitRun);
```
변경 후:
```js
    settingsPanel.show(sensitivity, handleSensitivityChange, closeSettings, exitRun, replayTutorial);
```

메뉴에서 연 쪽은 `onExit` 자리에 `undefined` 를 넘긴다 — 메뉴에는 `메뉴로` 버튼이
붙으면 안 되기 때문이다.

- [ ] **Step 13: 테스트와 빌드**

Run: `npm test`
Expected: 177 tests passed

Run: `npm run build`
Expected: 성공

- [ ] **Step 14: 하드코딩 색이 없는지 확인**

Run: `grep -n "#[0-9a-fA-F]\{3,8\}" game/src/ui/tutorialPrompt.js game/src/ui/settingsPanel.js`
Expected: 출력 없음 (exit 1)

- [ ] **Step 15: 커밋**

```bash
git add game/src/ui/tutorialPrompt.js game/src/ui/theme.css game/src/ui/settingsPanel.js game/src/gameplay/game.js
git commit -m "feat: teach the basic shot on the first run

Press-to-aim and release-to-fire is the opposite of the usual convention,
and the one line that explained it lived on the menu and vanished when the
game started. The reload lock had no explanation at all.

The prompt takes a step name every frame and only touches the DOM when the
name changes, so the frame loop can call it unconditionally. Hiding it uses
the same path: a step name with no text is simply not shown.

reloadFinished fires on the falling edge of isReloading, which is why the
loop now keeps the previous frame's value — reading it twice in one frame
would have fired the event on every frame of the reload instead of once.

The replay button is offered from the menu as well as from play, unlike
'back to menu', because a controls explanation is worth seeing again from
anywhere."
```

- [ ] **Step 16: 사용자 육안 확인 요청**

이 세션 환경에서는 브라우저 스크린샷이 실패한다 (`the Browser pane is not displayed`).
다음을 사용자에게 확인해 달라고 요청한다 (직접 브라우저를 열려고 하지 말 것):

1. `localStorage` 를 비우고 새로 시작하면 하단에 `마우스를 누른 채로 조준하세요` 가 뜨는가
2. 누르면 `손을 떼면 발사됩니다` 로 바뀌는가
3. 쏘면 `장전 중에는 조준할 수 없습니다` 로 바뀌고, 장전이 끝나면 다음 문구로 가는가
4. 라운드를 깨면 문구가 사라지고 다시 안 나오는가
5. 두 번째 판에서는 처음부터 안 뜨는가
6. 설정에 `튜토리얼 다시 보기` 가 있고, 플레이 중에 누르면 그 판에서 바로 1단계가 뜨는가
7. 메뉴에서 열었을 때 `메뉴로` 버튼은 없고 `튜토리얼 다시 보기` 만 있는가
8. 문구가 조준선이나 우측 상단 `P` 힌트와 겹치지 않는가

`localStorage` 비우는 방법은 브라우저 콘솔에서:

```
localStorage.removeItem('shootshoot.tutorial')
```

개발 서버 주소는 `http://127.0.0.1:5174/` 다. 5173은 다른 프로젝트(TowerWar)가 잡고 있다.
